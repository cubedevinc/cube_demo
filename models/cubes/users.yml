cubes:
  - name: users
    sql_table: ECOM.USERS
    public: false

    dimensions:
      - name: id
        sql: "{CUBE}.ID"
        type: number
        primary_key: true

      - name: state
        sql: STATE
        type: string

      - name: city
        sql: CITY
        type: string

      - name: gender
        sql: GENDER
        type: string

      - name: last_name
        sql: {{ masked('LAST_NAME', COMPILE_CONTEXT.securityContext) }}
        type: string

      - name: first_name
        sql: {{ masked('FIRST_NAME', COMPILE_CONTEXT.securityContext) }}
        type: string

      - name: created_at
        sql: CREATED_AT
        type: time

    measures:
      - name: count
        type: count